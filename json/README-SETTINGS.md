# 📖 Документация по настройкам JSON проектов

## 🎨 Структура проекта

```json
{
  "projects": [
    {
      "id": "уникальный-id",
      "name": "Название проекта",
      "logo": "путь/к/логотипу.png",
      "watermark": "Текст водяного знака",
      "logoSettings": { ... },
      "slides": [ ... ]
    }
  ]
}
```

---

## 🖼️ Настройки логотипа (logoSettings)

| Параметр | Тип | Описание | Пример |
|----------|-----|----------|--------|
| `height` | number | Высота логотипа в пикселях | `60` |
| `top` | number | Отступ сверху в пикселях | `40` |
| `left` | number | Отступ слева в пикселях | `40` |
| `opacity` | number | Прозрачность (0-1) | `1` |
| `shadow` | string | Тень (CSS формат) | `"0px 2px 8px rgba(0,0,0,0.5)"` |
| `border` | boolean | Показывать рамку | `false` |
| `borderWidth` | number | Толщина рамки в пикселях | `0` |
| `borderColor` | string | Цвет рамки (HEX) | `"#ffffff"` |
| `borderRadius` | number | Скругление углов в пикселях | `0` |

**Пример:**
```json
"logoSettings": {
  "height": 60,
  "top": 40,
  "left": 40,
  "opacity": 1,
  "shadow": "0px 2px 8px rgba(0, 0, 0, 0.5)",
  "border": false,
  "borderWidth": 0,
  "borderColor": "#ffffff",
  "borderRadius": 0
}
```

---

## 📄 Структура слайда

```json
{
  "id": 1,
  "background": { ... },
  "emoji": "🎯",
  "title": { ... },
  "subtitle": { ... },
  "paragraph": { ... },
  "images": [ ... ],
  "containerSettings": { ... },
  "cta": { ... }
}
```

---

## 🎨 Фон слайда (background)

### Градиентный фон
```json
"background": {
  "type": "gradient",
  "colors": ["#1e3a8a", "#3b82f6"],
  "direction": 135
}
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| `type` | string | Тип фона: `"gradient"` |
| `colors` | array | Массив из 2 цветов в HEX формате |
| `direction` | number | Направление градиента в градусах (0-360) |

**Популярные направления:**
- `0` - снизу вверх ↑
- `90` - слева направо →
- `135` - из левого нижнего в правый верхний ↗
- `180` - сверху вниз ↓

### Одноцветный фон
```json
"background": {
  "type": "solid",
  "colors": ["#1a1a1a"]
}
```

**⚠️ Важно:** Для одноцветного фона используйте массив с одним цветом в параметре `colors`!

---

## 📝 Текстовые элементы

### Заголовок (title)
```json
"title": {
  "text": "Заголовок слайда\nМожет быть многострочным",
  "tag": "h1",
  "style": {
    "fontFamily": "Geologica",
    "fontSize": "52px",
    "textAlign": "center",
    "color": "#ffffff"
  }
}
```

| Параметр | Значение | Описание |
|----------|----------|----------|
| `tag` | `"h1"` или `"h2"` | Тег заголовка |
| `text` | string | Текст (используйте `\n` для переноса строки) |

### Подзаголовок (subtitle)
```json
"subtitle": {
  "text": "Подзаголовок для дополнительной информации",
  "style": {
    "fontFamily": "Geologica",
    "fontSize": "26px",
    "textAlign": "center",
    "color": "#ffffff"
  }
}
```

### Параграф (paragraph)
```json
"paragraph": {
  "text": "Основной текст с подробным описанием",
  "style": {
    "fontFamily": "Geologica",
    "fontSize": "22px",
    "textAlign": "center",
    "color": "#ffffff"
  }
}
```

**💡 Совет:** Используйте `null` для элементов, которые не нужны на слайде.

---

## 🖼️ Изображения (images)

```json
"images": [
  {
    "src": "images/photo1.jpg",
    "alt": "Описание изображения",
    "size": 60
  },
  {
    "src": "images/photo2.jpg",
    "alt": "Описание изображения 2",
    "size": 45
  }
]
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| `src` | string | Путь к изображению |
| `alt` | string | Альтернативный текст |
| `size` | number | Размер изображения в % (рекомендуется 30-70) |

---

## 📦 Настройки контейнера изображений (containerSettings)

```json
"containerSettings": {
  "gap": 20,
  "align": "center",
  "direction": "row",
  "radius": 10
}
```

| Параметр | Тип | Возможные значения | Описание |
|----------|-----|-------------------|----------|
| `gap` | number | 0-50 | Расстояние между изображениями в пикселях |
| `align` | string | `"center"`, `"flex-start"`, `"flex-end"`, `"space-between"`, `"space-around"` | Выравнивание изображений |
| `direction` | string | `"row"`, `"column"` | Направление расположения |
| `radius` | number | 0-30 | Скругление углов изображений в пикселях |

### Примеры выравнивания (align)

**center** - по центру
```
    [IMG] [IMG]
```

**space-between** - по краям с равным расстоянием
```
[IMG]         [IMG]
```

**flex-start** - слева (или сверху для column)
```
[IMG] [IMG]
```

**flex-end** - справа (или снизу для column)
```
         [IMG] [IMG]
```

### Примеры direction

**row** - горизонтально (в ряд)
```
[IMG] [IMG] [IMG]
```

**column** - вертикально (в столбик)
```
[IMG]
[IMG]
[IMG]
```

---

## 🎯 Кнопка призыва к действию (CTA)

```json
"cta": {
  "text": "Начать →",
  "color": "#0f766e"
}
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| `text` | string | Текст на кнопке |
| `color` | string | Цвет текста кнопки в HEX |

**💡 Совет:** Используйте эмодзи в тексте для привлекательности: `"Создать игру →"`, `"Начать ✨"`

---

## 📏 Рекомендуемые размеры текста

### Формат 1:1 (500×500px)
- **Заголовок (h1, h2)**: 52px
- **Подзаголовок (subtitle)**: 26px
- **Параграф (p)**: 22px
- **Эмодзи**: 80px

### Формат 4:5 (500×625px)
- **Заголовок (h1, h2)**: 44px
- **Подзаголовок (subtitle)**: 20px
- **Параграф (p)**: 20px
- **Эмодзи**: 65px

### Формат 9:16 (405×720px - Stories)
- **Заголовок (h1, h2)**: 32px
- **Подзаголовок (subtitle)**: 16-18px
- **Параграф (p)**: 16px
- **Эмодзи**: 45px

---

## 🎨 Популярные цветовые комбинации для градиентов

| Название | Цвета | Пример |
|----------|-------|--------|
| Синий океан | `["#1e3a8a", "#3b82f6"]` | 🌊 |
| Фиолетовый закат | `["#7c3aed", "#a855f7"]` | 🌅 |
| Бирюзовый бриз | `["#0891b2", "#06b6d4"]` | 💎 |
| Зеленый лес | `["#059669", "#10b981"]` | 🌲 |
| Красный огонь | `["#dc2626", "#f43f5e"]` | 🔥 |
| Синий сапфир | `["#1e40af", "#3b82f6"]` | 💠 |
| Бирюзовый аквамарин | `["#0f766e", "#14b8a6"]` | 🏝️ |

---

## 📋 Полный пример слайда со всеми настройками

```json
{
  "id": 1,
  "background": {
    "type": "gradient",
    "colors": ["#1e3a8a", "#3b82f6"],
    "direction": 135
  },
  "emoji": "🎯",
  "title": {
    "text": "Заголовок слайда\nМожет быть многострочным",
    "tag": "h1",
    "style": {
      "fontFamily": "Geologica",
      "fontSize": "52px",
      "textAlign": "center",
      "color": "#ffffff"
    }
  },
  "subtitle": {
    "text": "Подзаголовок для дополнительной информации",
    "style": {
      "fontFamily": "Geologica",
      "fontSize": "26px",
      "textAlign": "center",
      "color": "#ffffff"
    }
  },
  "paragraph": {
    "text": "Основной текст с подробным описанием содержимого слайда",
    "style": {
      "fontFamily": "Geologica",
      "fontSize": "22px",
      "textAlign": "center",
      "color": "#ffffff"
    }
  },
  "images": [
    {
      "src": "images/photo1.jpg",
      "alt": "Первое изображение",
      "size": 50
    },
    {
      "src": "images/photo2.jpg",
      "alt": "Второе изображение",
      "size": 50
    }
  ],
  "containerSettings": {
    "gap": 20,
    "align": "center",
    "direction": "row",
    "radius": 10
  },
  "cta": {
    "text": "Начать →",
    "color": "#0f766e"
  }
}
```

---

## 💡 Полезные советы

### Размеры изображений (параметр size)
- **1 картинка**: 45-55%
- **2 картинки в ряд**: 40-45%
- **3 картинки в ряд**: 28-32%
- **2 картинки в столбик**: 40-45%

### Текст
1. **Многострочный текст**: Используйте `\n` для переноса строк в заголовках и тексте
2. **Краткость**: Старайтесь делать заголовки короткими (1-3 слова), подписи - до 1 предложения
3. **Эмодзи**: Добавляйте эмодзи для визуальной привлекательности
4. **Контраст**: Убедитесь, что текст хорошо читается на фоне

### Контейнер изображений

**Режимы позиционирования:**
- **📐 Flex Layout** - используйте JSON настройки ниже для автоматического выравнивания
- **✋ Свободное** - в редакторе переключите режим и перетаскивайте картинки мышью куда хотите

**Настройки Flex Layout:**
5. **Gap**: 8-15px для обычных изображений, 20-30px для space-between
6. **Radius**: Скругленные углы (8-12px) делают дизайн современнее
7. **Direction**:
   - `row` - для горизонтального расположения (картинки в ряд)
   - `column` - для вертикального расположения (картинки в столбик)
8. **Align**:
   - `center` - по центру (для большинства случаев)
   - `space-between` - по краям (для акцентов)
   - `flex-start` - слева/сверху
   - `flex-end` - справа/снизу

**Свободное позиционирование:**
- Доступно через кнопку "✋ Свободное" в панели настроек контейнера
- Перетаскивайте картинки мышью в любое место
- Позиции автоматически сохраняются и синхронизируются между форматами
- В этом режиме настройки `gap`, `align`, `direction` не действуют

---

## 🔧 Как использовать

1. Откройте файл `project-example-all-settings.json` чтобы увидеть примеры
2. Скопируйте нужный слайд и измените под свои нужды
3. Импортируйте JSON в редактор через кнопку "Импорт JSON"
4. Экспортируйте готовый проект через кнопку "Сохранить JSON"

---

**Создано для GameInvitation Marketing Generator**
