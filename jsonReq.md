# Формат JSON для импорта слайдов

## Структура файла

JSON файл содержит массив проектов. Каждый проект - это набор слайдов с настройками.

```json
{
  "projects": [
    {
      "id": "project-1",
      "name": "Новогодняя кампания 2025",
      "logo": "images/Logo/Logo GameInvitation.png",
      "watermark": "GameInvitation.com",
      "logoSettings": {
        "height": 60,
        "top": 40,
        "left": 40,
        "opacity": 1,
        "shadow": "0px 2px 8px rgba(0, 0, 0, 0.5)",
        "border": false,
        "borderWidth": 0,
        "borderColor": "#ffffff",
        "borderRadius": 0
      },
      "slides": [
        {
          "id": 1,
          "background": {
            "type": "gradient",
            "colors": ["#1e3a8a", "#3b82f6"],
            "direction": 135
          },
          "emoji": "🎄",
          "title": {
            "text": "До Нового года\n15 дней.\nА ты всё ещё\nбез подарков?",
            "tag": "h1"
          },
          "subtitle": {
            "text": "Покажу, как за 5 минут создать подарок,\nкоторый запомнят больше, чем шампанское"
          },
          "images": [
            {
              "src": "images/Новогодняя-открытка-90ые.webp",
              "alt": "Новогодняя открытка",
              "size": 60
            }
          ],
          "containerSettings": {
            "gap": 15,
            "align": "center",
            "radius": 0
          },
          "cta": null
        }
      ]
    }
  ]
}
```

## Описание полей

### Корневой объект
- `projects` (array) - массив проектов

### Проект (project)
- `id` (string) - уникальный идентификатор проекта
- `name` (string) - название проекта для отображения
- `logo` (string) - путь к логотипу
- `watermark` (string) - текст водяного знака
- `logoSettings` (object|null) - настройки логотипа (опционально)
  - `height` (number) - высота логотипа в пикселях (по умолчанию: 60)
  - `top` (number) - позиция сверху в пикселях (по умолчанию: 40)
  - `left` (number) - позиция слева в пикселях (по умолчанию: 40)
  - `opacity` (number) - прозрачность от 0 до 1 (по умолчанию: 1)
  - `shadow` (string) - тень в формате CSS drop-shadow (например, "0px 2px 8px rgba(0, 0, 0, 0.5)")
  - `border` (boolean) - включена ли обводка (по умолчанию: false)
  - `borderWidth` (number) - толщина обводки в пикселях (по умолчанию: 0)
  - `borderColor` (string) - цвет обводки в HEX формате (по умолчанию: "#ffffff")
  - `borderRadius` (number) - радиус скругления обводки в пикселях (по умолчанию: 0)

### Слайд (slide)
- `id` (number) - номер слайда (1, 2, 3...)
- `background` (object) - настройки фона
  - `type` (string) - тип фона: "gradient" или "solid"
  - `colors` (array) - массив цветов для градиента или один цвет для solid
  - `direction` (number) - направление градиента в градусах (для gradient)
- `emoji` (string|null) - эмоджи для слайда (может быть null)
- `title` (object) - заголовок слайда
  - `text` (string) - текст заголовка (поддерживает \n для переносов)
  - `tag` (string) - HTML тег: "h1" или "h2"
  - `style` (object|null) - стили текста (опционально)
    - `fontFamily` (string|null) - семейство шрифта (например, "Arial, sans-serif")
    - `fontSize` (string|null) - размер шрифта (например, "24px")
    - `textAlign` (string|null) - выравнивание текста: "left", "center", "right", "justify"
    - `color` (string|null) - цвет текста в HEX формате (например, "#ffffff" для белого, "#000000" для черного)
- `subtitle` (object|null) - подзаголовок (опционально)
  - `text` (string) - текст подзаголовка
  - `style` (object|null) - стили текста (опционально)
    - `fontFamily` (string|null) - семейство шрифта
    - `fontSize` (string|null) - размер шрифта
    - `textAlign` (string|null) - выравнивание текста
- `paragraph` (object|null) - обычный текст (опционально)
  - `text` (string) - текст параграфа
  - `style` (object|null) - стили текста (опционально)
    - `fontFamily` (string|null) - семейство шрифта
    - `fontSize` (string|null) - размер шрифта
    - `textAlign` (string|null) - выравнивание текста
- `images` (array) - массив изображений
  - `src` (string) - путь к изображению или base64 строка
  - `alt` (string) - альтернативный текст
  - `size` (number|null) - размер изображения в процентах (опционально, для загруженных изображений)
- `containerSettings` (object|null) - настройки контейнера изображений (опционально)
  - `gap` (number) - отступ между изображениями в пикселях (по умолчанию: 15)
  - `align` (string) - выравнивание изображений: "flex-start" (слева), "center" (по центру), "flex-end" (справа), "space-between" (распределить) (по умолчанию: "center")
  - `radius` (number) - радиус скругления углов изображений в пикселях (по умолчанию: 0)
- `cta` (object|null) - кнопка призыва к действию (опционально)
  - `text` (string) - текст кнопки
  - `color` (string) - цвет текста кнопки

## Пример полного файла

```json
{
  "projects": [
    {
      "id": "project-1",
      "name": "Новогодняя кампания 2025",
      "logo": "images/Logo/Logo GameInvitation.png",
      "watermark": "GameInvitation.com",
      "logoSettings": {
        "height": 60,
        "top": 40,
        "left": 40,
        "opacity": 1,
        "shadow": "0px 2px 8px rgba(0, 0, 0, 0.5)",
        "border": false,
        "borderWidth": 0,
        "borderColor": "#ffffff",
        "borderRadius": 0
      },
      "slides": [
        {
          "id": 1,
          "background": {
            "type": "gradient",
            "colors": ["#1e3a8a", "#3b82f6"],
            "direction": 135
          },
          "emoji": "🎄",
          "title": {
            "text": "До Нового года\n15 дней.\nА ты всё ещё\nбез подарков?",
            "tag": "h1"
          },
          "subtitle": {
            "text": "Покажу, как за 5 минут создать подарок,\nкоторый запомнят больше, чем шампанское"
          },
          "images": [],
          "containerSettings": {
            "gap": 15,
            "align": "center",
            "radius": 0
          }
        },
        {
          "id": 2,
          "background": {
            "type": "gradient",
            "colors": ["#7c3aed", "#a855f7"],
            "direction": 135
          },
          "emoji": "💸",
          "title": {
            "text": "Сертификат в конверте —\nуже не торт",
            "tag": "h2"
          },
          "paragraph": {
            "text": "Представь: человек открывает твоё поздравление. Там ссылка. Он переходит — а там игра. С вашими фото из этого года. Он проходит квест — и только в конце получает твой подарок. Вот это Новый год."
          },
          "images": [
            {
              "src": "images/prazdnichnyy-onlayn-syurpriz-novogodniy-blesk_reg022_1.jpg",
              "alt": "Новогодний сюрприз"
            }
          ],
          "cta": null
        }
      ]
    },
    {
      "id": "project-2",
      "name": "Весенняя акция",
      "logo": "images/Logo/Logo GameInvitation.png",
      "watermark": "GameInvitation.com",
      "slides": [
        {
          "id": 1,
          "background": {
            "type": "solid",
            "colors": ["#10b981"]
          },
          "emoji": "🌸",
          "title": {
            "text": "Весна пришла!",
            "tag": "h1"
          },
          "subtitle": null,
          "paragraph": null,
          "images": [],
          "cta": null
        }
      ]
    }
  ]
}
```

## Редактирование текста

В интерфейсе генератора доступно редактирование текстовых элементов:
- Клик по любому текстовому элементу (заголовок, подзаголовок, параграф) открывает панель редактирования
- В панели можно изменить:
  - **Шрифт**: выбор из предустановленных шрифтов (Arial, Georgia, Times New Roman, Courier New, Verdana, Trebuchet MS, Impact, Comic Sans MS, Roboto, Open Sans, Montserrat, Playfair Display)
  - **Размер шрифта**: от 8px до 200px
  - **Выравнивание**: по левому краю, по центру, по правому краю, по ширине
- Изменения автоматически применяются ко всем форматам слайда (1:1 и 9:16)
- Все изменения сохраняются в JSON при экспорте

## Управление изображениями

- Изображения можно загружать через кнопку "📷 Загрузить изображение"
- При загрузке автоматически вычисляется оптимальный размер, чтобы изображение не выходило за границы слайда
- Размер изображения можно изменить через контекстное меню (клик по изображению → "📏 Изменить размер")
- Изображения можно перетаскивать для изменения порядка
- Размер изображения сохраняется в JSON (поле `size` в процентах)
- В панели загрузки отображаются все изображения слайда с возможностью удаления
- Настройки контейнера изображений доступны через кнопку "⚙️ Настройки" в панели загрузки:
  - **Отступы между изображениями**: от 0 до 100px
  - **Выравнивание**: по левому краю, по центру, по правому краю, распределить
  - **Округление углов**: от 0 до 50px (применяется к контейнеру и изображениям)

## Управление логотипом

- Клик по логотипу открывает панель настроек
- Доступные настройки:
  - **Размер (высота)**: от 20 до 200px
  - **Позиция сверху**: от 0 до 200px
  - **Позиция слева**: от 0 до 500px
  - **Прозрачность**: от 0 до 100%
  - **Тень**: настройка X, Y, размытия, цвета и прозрачности тени
  - **Обводка**: толщина, цвет и радиус скругления
- Все настройки применяются ко всем слайдам проекта автоматически
- Настройки сохраняются в JSON в объекте `logoSettings` проекта

## Примечания

- Все пути к изображениям относительные от корня проекта или base64 строки
- Эмоджи поддерживают любые Unicode символы
- Текст поддерживает переносы строк через `\n`
- Если поле опционально и отсутствует, используйте `null`
- Минимальное количество слайдов в проекте: 1
- Максимальное количество слайдов не ограничено
- Стили текста применяются ко всем форматам слайда автоматически
- Размеры изображений сохраняются и восстанавливаются при импорте JSON
- Настройки контейнера изображений сохраняются в поле `containerSettings` каждого слайда
- Настройки логотипа сохраняются в поле `logoSettings` проекта и применяются ко всем слайдам проекта
- Все настройки (логотип, контейнер изображений, стили текста) автоматически применяются при импорте JSON
